# Post Scheduler

A post scheduling app built with a Ruby on Rails backend and a React frontend.

## Setup

### Backend

```bash
cd backend
bundle install
bin/rails db:create db:migrate db:seed
bin/rails s
```

### Frontend

```bash
cd frontend
npm install
npm run dev
```

Go to `http://localhost:5173` to view the application.
